# Handoff — 2026-04-25 (late night session)

> **Read this first in the new chat.** Then pick up from "Next session — proposed plan."

---

## Quick state snapshot

**What's running right now:**
- Backend (FastAPI) on `:8000` — boots without API key (lazy Anthropic init)
- UI (Next.js cockpit) on `:3000` — http://localhost:3000/cockpit
- Postgres (`postgresql@15`) — auto-starts on boot
- Ollama (`nomic-embed-text`) on `:11434` — auto-starts on boot
- `memory` MCP server registered in `~/.claude/settings.json` — restart Claude Code session to pick up the new tools

**What's NOT running but should be flagged:**
- ffmpeg upscale was killed earlier (was eating 265% CPU)
- Stale `vampire-sex` Next.js servers were killed; auto-launch disabled by renaming `~/Downloads/vampire-sex street/.claude/launch.json` → `launch.json.disabled`

**Test state:**
- Backend pytest: 166 passing
- UI vitest: 33 passing
- Total: 199 tests, zero failures

**Latest commit:** `173ce20` — `.env.example` slots for Spotify/Songstats/Chartmetric/Instagram/Meta/etc., pushed to `origin/main`

**Memory system:**
- 31 entries in LanceDB, 12 domain code tags propagated
- Hebbian: 17 pairs tracked, 1 already tightened (`BEHAVIOR ↔ IDENTITY` ring 3 → 2)
- Today's micro file exists: `~/.claude/memory/micro/2026-04/2026-04-25.jsonl` (1 entry: deferred Haiku swap decision)

---

## What got built tonight (Apr 24-25)

### Memory architecture (the big one)
1. **Hebbian co-occurrence write-side** — `~/.claude/scripts/memory_hebbian.py` (250 lines), `test_memory_hebbian.py` (11/11 passing). CLI: `increment`, `tighten`, `list`, `ring`.
2. **Hebbian read-side** — `~/.claude/memory-mcp/topology.js` (150 lines, mtime-cached), `test_topology.js` (17 assertions passing).
3. **Three new MCP tools** in `server.js`:
   - `ring_neighbors(code)` — topology debug
   - `recall_neighborhood(codes, limit)` — explicit ring-walk retrieval
   - `recall(query, limit, codes?)` — vector + ring-bias fusion
4. **Vector score normalization** — `searchMemories()` now returns `similarity` (query-local min-max [0,1]) plus raw `distance` for debug. Fixes the L2-distance vs cosine-bias scale mismatch.
5. **Indexer extension** — `index-memories.js` derives 12 domain codes via regex from content/source, injects them into the `tags` array. 31 memories now carry codes.
6. **Capture-loop hook** — `~/.claude/hooks/memory-capture-reminder` extended to also tell Claude to run `memory_hebbian.py increment ...` and `tighten` at session-end.

### Mission Control Phase 3 closure
- Phase 2 ABAC consolidation closed at 100%
- Tailwind v3 installed with brand-aware theme
- Vitest scaffold + 33 component tests
- React 18 `act()` warnings silenced
- Lazy-init Anthropic client (backend boots without key)
- CORS regex for any localhost port (autoPort fallback compat)
- Dev-server bootstrap via `.claude/launch.json`
- Cockpit verified end-to-end (Mission created via real `POST /api/missions`)

### Misc.
- Daily Mission Architect cron at 8:57 AM weekdays via launchd plist
- Meta API setup reminder on **both Google Calendars** (londonxofc + teamnicklondon) for Sun Apr 26, 4:00 PM
- 📞 Call Ana reminder on **both Google Calendars** for 2:00 AM Apr 26
- Ana Ayora added to `~/.claude/people.md` Family section (and project `CLAUDE.md`) with full bio
- `.env.example` extended with tiered API key slots (Spotify, Songstats, Chartmetric, Instagram+Meta, Vercel/Railway, OpenAI/ElevenLabs)
- Memory MCP indexer fixed (path bug, malformed YAML guard, Ollama context truncation)

---

## What's deferred / open

| Priority | Item | Why |
|----------|------|-----|
| 🔴 High | Get Anthropic API key into `.env` | Mission Control can't run real missions without it |
| 🟡 Med | Fractal memory: capture loop never fired in production | Stop hook is wired but today was build-mode, no real captures yet. Worth dogfooding. |
| 🟡 Med | Promotion path (micro→episode→project→identity) | Schema supports it, `memory_promote.py` exists, but unexercised |
| 🟡 Med | Auto-Hebbian increment | Currently Claude has to manually list active codes. Could be automated by reading session activity. |
| 🟢 Low | ElevenLabs key already created — paste into `.env` as `ELEVENLABS_API_KEY=` |
| 🟢 Low | Spotify Web API setup (free, ~10 min) — unlocks VS/LX track analytics |
| 🟢 Low | Songstats API (you may have account; check) — direct fulfillment of CLAUDE.md Data Protocol |
| 🟢 Low | Wakanda tool registry entries — needs Nick's input on ATS workflow |
| 🟢 Low | Resonance OS architectural call — keep memory in `.claude/`, or migrate to Resonance OS as a service? |

---

## Next session — proposed plan

Nick's framing: "**operate autonomously while I work on something else, make every decision as if you were me.**"

**Recommended order (60 min budget):**

### 1. Promotion dry-run (15 min, read-only)
Run `python3 ~/.claude/scripts/memory_promote.py --dry-run` against the existing 11 micro entries. Report what would graduate to episode / project. Per CLAUDE.md rule: **identity-level graduations require Nick's approval** — propose, don't commit.

### 2. Capture this session's signals (15 min, write to micro)
Use `capture-memory` skill heuristics strictly. Likely candidates from tonight:
- DECISION: Hebbian θ=5 threshold validated by first real tightening event
- PROCEDURE: ring-aware retrieval pattern (vector + topology fusion)
- PREFERENCE: Nick prefers "operate as if you were me" autonomy mode for closing-out work
- FACT: Ana Ayora is Nick's sister, actress (added to people.md)
- DECISION: ElevenLabs key scoped to creative endpoints only, $10/mo cap
- DECISION: Mission Control Anthropic key deferred — Phase 3 done, no need until live missions

Don't over-capture. Aim for 3-5 entries max. Salience anchored low per heuristic.

### 3. Run Hebbian increment for tonight (5 min)
Codes that genuinely co-occurred:
- `BUILDS BEHAVIOR TOOLS LEARNING` — definite (memory architecture work)
- `IDENTITY PEOPLE` — Ana addition
- `BOUNDARIES` — security hooks blocking .env

Then `tighten`. Report any new ring transitions.

### 4. Auto-Hebbian increment build (25 min, optional)
Small Python script: `memory_hebbian_session.py` that reads recent Claude Code session log files and extracts active codes via the same regex map already in `index-memories.js`. Reduces manual work at session-close. Tests in isolation.

**Stop conditions** (ask Nick before proceeding):
- Identity-level promotion candidate
- Schema change that'd break existing entries
- Anything that needs $ or external API call
- Conflicting evidence between two sources

**End-of-session report:** what landed, what was deferred, what to review.

---

## Drift watchlist (where I'm most likely to mess up operating as Nick)

1. **Scope Gate** — risk of writing project-specific stuff into durable identity files. Sanity check: every entry must pass *"Is this true regardless of what project Nick is working on?"*
2. **Salience inflation** — I tend to score things higher than the heuristic. Anchor at 0.20, only add per the rules.
3. **Brand/quality calls** — anything touching VS/LX/ATS positioning needs Nick. Flag and defer.
4. **Demonstration-mode blind spot** (Apr 15 correction) — the act of explaining a guardrail can suspend it. Every line written to CLAUDE.md / people.md must pass Scope Gate, including examples.
5. **Plausibility ≠ truth** (Apr 18 correction) — for any real-world entity name in a saved file, verify via search before writing.

If I drift, log it in the corrections section of `~/.claude/CLAUDE.md` immediately.

---

## To pick up cleanly in the new chat

Open a fresh Claude Code session and paste:

```
Read /Users/Malachi/Missipn Control Builder Agent/HANDOFF-2026-04-25.md and pick up from "Next session — proposed plan." Operate autonomously per the plan. Stop conditions and drift watchlist apply.
```

The new session will have:
- Fresh context window
- Same memory MCP tools (will pick up `recall`, `recall_neighborhood`, `ring_neighbors` after MCP server restart)
- Same Hebbian state (`pairs.jsonl` persists)
- Same daily 8:57 AM cron (launchd, persists)

Note: the in-session crons (📞 Call Ana at 2 AM, Meta API reminder Sun 4 PM) are session-only and will die when this chat closes. **Google Calendar copies are the real reminders** — both already saved on both calendars.

---

**End handoff.**
